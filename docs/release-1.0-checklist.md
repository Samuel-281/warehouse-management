# 1.0 发布检查清单

## 发布前

- [ ] 登录页不展示测试账号和默认密码。
- [ ] 生产环境 `.env` 未提交到 Git。
- [ ] 网页端数据重置入口仅超级管理员可见，且必须输入确认文字。
- [ ] `ALLOW_DEMO_PASSWORD_LOGIN` 保持为 `false`。
- [ ] 已运行 `npm run lint`。
- [ ] 已运行 `npm run build`。
- [ ] 已运行 `npm run prisma:validate`。
- [ ] 超级管理员、仓库管理员、只读查询人员权限已回归验证。
- [ ] 入库、出库、销售退回、库存查询、单据导出已回归验证。

## 部署前

- [ ] ECS 安全组只开放必要端口。
- [ ] PostgreSQL 只允许本机连接。
- [ ] Nginx 已反向代理到 `127.0.0.1:3000`。
- [ ] PM2 已设置进程守护并保存。
- [ ] 数据库初始化和 seed 已执行。
- [ ] 初始账号密码已更换为强密码。
- [ ] 每日数据库备份已配置。

## 发布标记

通过以上检查后创建版本标签：

```bash
git tag v1.0.0
```
